using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class Health : MonoBehaviour
{
    

    private void Update()
    {
        HeartUpdater();
    }

    //function that updates the player's health hud
    private void HeartUpdater()
    {
        //for(i = 0; i < damage; i++){

        //h[i].gameObject.SetActive(false);
        //e[i].gameObject.SetActive(true);
        //i++
        //}
      
    }
}
